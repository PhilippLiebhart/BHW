const { atom } = require("recoil");

export const tagesgericht = atom({
  key: "tagesgericht",
  default: [],
});

export const tagesgerichte = atom({
  key: "tagesgerichte",
  default: [],
});

export const getraenke = atom({
  key: "getraenke",
  default: [],
});
